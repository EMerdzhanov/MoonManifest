import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const deepIndigo = Color(0xFF0A0E1A);
  static const darkNavy = Color(0xFF0F1527);
  static const midNavy = Color(0xFF1A2040);
  static const cardDark = Color(0xFF151B30);

  // Moon
  static const moonSilver = Color(0xFFE8E8F0);
  static const moonWhite = Color(0xFFF5F5FA);
  static const moonGlow = Color(0x40C8C8E0);

  // Accent
  static const mutedGold = Color(0xFFD4A574);
  static const softGold = Color(0xFFE8C89E);
  static const dimGold = Color(0x40D4A574);

  // Text
  static const textPrimary = Color(0xFFE8E8F0);
  static const textSecondary = Color(0xFF9098B8);
  static const textMuted = Color(0xFF5A6280);

  // Functional
  static const surface = Color(0xFF1A2040);
  static const error = Color(0xFFCF6679);
  static const success = Color(0xFF81C784);
}
