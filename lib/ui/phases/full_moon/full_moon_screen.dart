import 'package:flutter/material.dart';

class FullMoonScreen extends StatelessWidget {
  const FullMoonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Full Moon')));
  }
}
