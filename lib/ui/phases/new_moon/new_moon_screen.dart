import 'package:flutter/material.dart';

class NewMoonScreen extends StatelessWidget {
  const NewMoonScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('New Moon')));
  }
}
