import 'package:flutter/material.dart';

class WaningScreen extends StatelessWidget {
  const WaningScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Waning')));
  }
}
