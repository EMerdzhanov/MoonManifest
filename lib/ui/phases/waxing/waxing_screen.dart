import 'package:flutter/material.dart';

class WaxingScreen extends StatelessWidget {
  const WaxingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Waxing')));
  }
}
